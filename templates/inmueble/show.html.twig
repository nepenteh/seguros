{% extends 'base.html.twig' %}

{% block title %}Inmueble{% endblock %}

{% block body %}
    <h1>Inmueble</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ inmueble.id }}</td>
            </tr>
            <tr>
                <th>Calle</th>
                <td>{{ inmueble.calle }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ inmueble.cp }}</td>
            </tr>
            <tr>
                <th>Localidad</th>
                <td>{{ inmueble.localidad }}</td>
            </tr>
            <tr>
                <th>Provincia</th>
                <td>{{ inmueble.provincia }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_inmueble_index') }}">back to list</a>

    <a href="{{ path('app_inmueble_edit', {'id': inmueble.id}) }}">edit</a>

    {{ include('inmueble/_delete_form.html.twig') }}
{% endblock %}

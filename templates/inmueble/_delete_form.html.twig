<form class="d-inline-block mx-4" method="post" action="{{ path('app_inmueble_delete', {'id': inmueble.id}) }}" onsubmit="return confirm('¿Confirma que desea eliminar el Inmueble?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inmueble.id) }}">
    <button class="btn btn-outline-danger py-1">Eliminar Inmueble</button>
</form>

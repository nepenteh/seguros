<form method="post" action="{{ path('app_inmueble_delete', {'id': inmueble.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inmueble.id) }}">
    <button class="btn">Delete</button>
</form>

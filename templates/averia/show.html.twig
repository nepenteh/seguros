{% extends 'base.html.twig' %}

{% block title %}Averia{% endblock %}

{% block body %}
<div class="container py-1 pt-3">
    <h1>Averia</h1>

    <table class="table table-striped mt-3">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ averium.id }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ averium.fecha ? averium.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ averium.descripcion }}</td>
            </tr>
            <tr>
                <th>Póliza</th>
                <td>{{ averium.poliza }}</td>
            </tr>
            <tr>
                <th>Aseguradora</th>
                <td>{{ averium.poliza.aseguradora }}</td>
            </tr>
            <tr>
                <th>Inmueble</th>
                <td>{{ averium.poliza.inmueble }}</td>
            </tr>

        </tbody>
    </table>

    <a class="btn btn-outline-dark py-1" href="{{ path('app_averia_index') }}">Volver al Listado</a>

    <a class="btn btn-outline-dark py-1" href="{{ path('app_averia_edit', {'id': averium.id}) }}">Edit</a>

    {{ include('averia/_delete_form.html.twig') }}
</div>
{% endblock %}

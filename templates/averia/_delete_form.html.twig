<form method="post" action="{{ path('app_averia_delete', {'id': averium.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ averium.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('app_aseguradora_delete', {'id': aseguradora.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ aseguradora.id) }}">
    <button class="btn">Delete</button>
</form>
